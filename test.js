function toNumSum(x, y) {
    const result = x + y
    console.log(result)
    return `${x} + ${y} = ${result}`
}

const name = 'Mohammad Anarul Islam'

const result = toNumSum(1, 40)
console.log(result)